import React from 'react';
import '../App.css';

const AllPropertiesButton = props => {
    return <button className="hpButton" onClick={props.goToProperties}>Properties</button>
}

export default AllPropertiesButton;
